Чтобы запостить **картинку с текстом** на страницу Facebook через API, нужно использовать **два варианта** в зависимости от того, как вы хотите оформить пост:

---

## **📌 Вариант 1: Пост с текстом и прикреплённой картинкой**  
*(Текст + отдельное изображение, как в обычном посте)*  

### **HTTP-запрос (Graph API)**
```bash
POST /v19.0/{page-id}/photos?  
    url={ссылка_на_изображение}&  
    message={текст_поста}&  
    access_token={page_access_token}
```

### **Пример через cURL**
```bash
curl -X POST \
"https://graph.facebook.com/v19.0/2120362494678794/photos" \
-F "url=https://example.com/your-image.jpg" \
-F "message=Текст поста с картинкой 📸" \
-F "access_token=EAA520SFRtvcBOZBIQ3dGlzOyUDFBLGxHncVH7FUBZAzzn0U4Il8pkikZCiixyMJbAyLEsfmeALPNf0QFHmkvvxzWX5KZC26wtZC597ZA3RAgZC9N60N3ZAQImCMdxRpry5rofkTy3Nhn14FZAgSX2wukgzF69x6Gu38akW89w9owTIePqZCZApInFJHwo36sarZAmZBF00d2BpZCNqsSOmw5ZAKaQV93m33"
```

### **Что важно?**
✅ **`url`** — прямая ссылка на изображение (должна быть доступна для загрузки Facebook).  
✅ **`message`** — текст поста (необязательно, но лучше добавить).  

---

## **📌 Вариант 2: Пост с текстом, встроенным в изображение**  
*(Если текст уже на картинке, например, мем или баннер)*  

### **HTTP-запрос (Graph API)**
```bash
POST /v19.0/{page-id}/feed?  
    message={текст_поста}&  
    attached_media[0]={"media_fbid":"{id_загруженной_картинки}"}&  
    access_token={page_access_token}
```

### **Как это работает?**  
1. **Сначала загрузите картинку** (получите её `media_fbid`):  
   ```bash
   curl -X POST \
   "https://graph.facebook.com/v19.0/2120362494678794/photos" \
   -F "source=@/путь/к/файлу.jpg" \
   -F "published=false" \
   -F "access_token=EAА..."
   ```
   - В ответе будет `id` загруженного изображения (например, `123456789`).  

2. **Запостите его с текстом:**  
   ```bash
   curl -X POST \
   "https://graph.facebook.com/v19.0/2120362494678794/feed" \
   -F "message=Текст поста с уже встроенной картинкой 🖼️" \
   -F "attached_media[0]={\"media_fbid\":\"123456789\"}" \
   -F "access_token=EAА..."
   ```

---

## **🔧 Частые ошибки и решения**  
❌ **Ошибка `(#100) URL is not properly formatted`** → Убедитесь, что ссылка на картинку **рабочая** и доступна извне.  
❌ **Ошибка `(#324) Requires upload file`** → Если загружаете файл (`@/path/to/image.jpg`), проверьте, что он существует.  
❌ **Ошибка `(#200) Permissions error`** → Токен не имеет прав `pages_manage_posts`.  

---

## **🚀 Дополнительно**  
- **Видео:** Замените `/photos` на `/videos`.  
- **Альбомы:** Используйте `/albums`, если нужно несколько фото.  
- **Ссылки:** Добавьте параметр `link`, если картинка — превью для URL.  

Если нужно **постить в Instagram** — используйте `instagram_content_publish`, но там свой API.  

**Теперь ты знаешь, как постить картинки с текстом!** 🎉  
Если что-то не работает — скинь ошибку, помогу разобрать. 😊