<think>**Планируя интеграцию Swagger**

- Давай подключим Swagger/OpenAPI для документации API, это упростит работу.
- Пользователь хочет, чтобы я помог с ТЗ для Swagger/OpenAPI.
</think>

Конечно, звучит как план! 😎 Подключение Swagger/OpenAPI — отличный шаг для твоего SMM Manager: это сделает API документацию интерактивной, самогенерируемой и удобной для разработчиков/тестеров. Поскольку проект на Express с TypeScript, я рекомендую использовать комбо из `swagger-jsdoc` (для генерации OpenAPI spec из JSDoc-комментариев в коде) и `swagger-ui-express` (для UI на /api-docs). Это просто интегрируется в монолит, не требует больших изменений и поддерживает твои endpoints (как /api/auth/login или /api/ai/generate-content).

Я подготовил готовое **ТЗ (техническое задание)** для Replit (или кого-то, кто реализует). Оно структурировано, с шагами, зависимостями и примерами кода. Ты можешь скопировать его целиком, доработать и передать. Если нужно, я могу сгенерировать полный код или протестировать его через мой интерпретатор.

### Техническое задание: Интеграция Swagger/OpenAPI в SMM Manager

**Версия ТЗ:** 1.0  
**Дата:** 17 августа 2025  
**Цель:** Добавить автоматическую генерацию и отображение API документации с использованием OpenAPI 3.0 spec. Это повысит покрытие документации до 95-100%, упростит onboarding и поможет в отладке.  
**Область применения:** Backend (Node.js/Express с TypeScript). Frontend не затрагивается.  
**Требуемые навыки:** TypeScript, Express, JSDoc.  
**Ожидаемое время реализации:** 4-6 часов (включая тестирование).  
**Зависимости от существующей архитектуры:**  
- Используем существующие endpoints (из документации: auth, ai, keywords и т.д.).  
- Интеграция в app.ts или server.ts (основной файл Express).  
- Существующий response format (APIResponse<T>) должен отражаться в spec.  
- Error codes (AUTH_REQUIRED и т.д.) включить в описания ошибок.

#### Шаг 1: Установка зависимостей
- Добавьте пакеты в `package.json`:
  ```
  npm install swagger-jsdoc swagger-ui-express @types/swagger-jsdoc @types/swagger-ui-express --save
  ```
- Для TypeScript добавьте типы (если не установлены):
  ```
  npm install @types/express --save-dev
  ```

#### Шаг 2: Конфигурация OpenAPI spec
- Создайте файл `swagger.ts` (или `openapi.config.ts`) в корне backend (рядом с app.ts).
- В нём определите базовую конфигурацию OpenAPI. Используйте JSDoc-комментарии в роутерах для автогенерации.
- Пример содержимого `swagger.ts`:
  ```typescript
  import swaggerJSDoc from 'swagger-jsdoc';

  const options: swaggerJSDoc.Options = {
    definition: {
      openapi: '3.0.0',
      info: {
        title: 'SMM Manager API',
        version: '1.0.0',
        description: 'API для управления SMM-кампаниями, AI-генерацией контента и мультиплатформенной публикацией',
        contact: {
          name: 'Your Team',
          email: 'support@example.com',
        },
      },
      servers: [
        {
          url: 'http://localhost:5000',
          description: 'Development server',
        },
        {
          url: 'https://your-production-url.com',
          description: 'Production server',
        },
      ],
      components: {
        securitySchemes: {
          bearerAuth: {
            type: 'http',
            scheme: 'bearer',
            bearerFormat: 'JWT',
          },
        },
        schemas: {
          APIResponse: {
            type: 'object',
            properties: {
              success: { type: 'boolean' },
              data: { type: 'object' }, // Generic, можно уточнить per endpoint
              error: {
                type: 'object',
                properties: {
                  message: { type: 'string' },
                  code: { type: 'string' },
                  details: { type: 'object' },
                },
              },
              meta: {
                type: 'object',
                properties: {
                  pagination: { type: 'object' }, // Если есть
                  timestamp: { type: 'string' },
                },
              },
            },
          },
          // Добавьте другие schemas, e.g., Campaign, AIModels enum и т.д.
          Campaign: {
            type: 'object',
            properties: {
              id: { type: 'string', format: 'uuid' },
              name: { type: 'string' },
              description: { type: 'string' },
              settings: { type: 'object' },
              created_at: { type: 'string', format: 'date-time' },
              updated_at: { type: 'string', format: 'date-time' },
              user_id: { type: 'string', format: 'uuid' },
            },
          },
          // Enum для AIModels
          AIModels: {
            type: 'string',
            enum: ['gemini-2.5-flash', 'gemini-2.0-flash-exp', 'deepseek-v3', 'claude-3.5-sonnet', 'fal-ai/*'],
          },
        },
      },
    },
    apis: ['./src/routes/*.ts', './src/controllers/*.ts'], // Пути к файлам с роутерами и контроллерами
  };

  const specs = swaggerJSDoc(options);
  export default specs;
  ```

#### Шаг 3: Интеграция в Express app
- В основном файле Express (app.ts или server.ts) добавьте:
  ```typescript
  import swaggerUi from 'swagger-ui-express';
  import specs from './swagger'; // Путь к файлу выше

  // ...

  app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(specs));
  ```
- Это сделает документацию доступной по `/api-docs` (e.g., http://localhost:5000/api-docs).

#### Шаг 4: Добавление JSDoc-комментариев к endpoints
- Для каждого роутера/контроллера добавьте JSDoc над функциями. Swagger будет генерировать spec на основе этого.
- Примеры для ключевых endpoints (из твоей документации):
  - Для `/api/auth/login`:
    ```typescript
    /**
     * @swagger
     * /api/auth/login:
     *   post:
     *     summary: Авторизация пользователя
     *     tags: [Authentication]
     *     requestBody:
     *       required: true
     *       content:
     *         application/json:
     *           schema:
     *             type: object
     *             properties:
     *               email: { type: string }
     *               password: { type: string }
     *     responses:
     *       200:
     *         description: Успешная авторизация
     *         content:
     *           application/json:
     *             schema:
     *               $ref: '#/components/schemas/APIResponse'
     *       401:
     *         description: Неправильные credentials
     *         content:
     *           application/json:
     *             schema:
     *               $ref: '#/components/schemas/APIResponse'
     *     security: [] // Нет auth для login
     */
    // Твой существующий handler
    ```
  - Для `/api/ai/generate-content`:
    ```typescript
    /**
     * @swagger
     * /api/ai/generate-content:
     *   post:
     *     summary: Генерация контента с использованием AI
     *     tags: [AI Generation]
     *     security:
     *       - bearerAuth: []
     *     requestBody:
     *       required: true
     *       content:
     *         application/json:
     *           schema:
     *             type: object
     *             properties:
     *               model: { $ref: '#/components/schemas/AIModels' }
     *               prompt: { type: string }
     *               maxTokens: { type: number, default: 3000 }
     *     responses:
     *       200:
     *         description: Сгенерированный контент
     *         content:
     *           application/json:
     *             schema:
     *               $ref: '#/components/schemas/APIResponse'
     *       500:
     *         description: Ошибка AI сервиса
     *     x-codegen-request-body-name: body
     */
    // Твой handler
    ```
- Аналогично для остальных: auth/refresh, ai/generate-image, keywords/analyze-website и т.д. Цель — покрыть все модули (auth, campaigns, ai, trends, publication и integrations).

#### Шаг 5: Тестирование и деплой
- Запустите сервер: `npm run dev`.
- Проверьте `/api-docs`: должна быть интерактивная UI с endpoints, schemas и try-it-out.
- Тестируйте: Попробуйте запросы прямо из Swagger UI.
- Edge cases: Убедитесь, что security (JWT) отражается, errors (e.g., VALIDATION_ERROR) описаны.
- Для production: Добавьте env var для скрытия /api-docs в dev-only (e.g., if (process.env.NODE_ENV === 'development') app.use(...)).
- Если нужно, экспортируйте spec в YAML/JSON: Добавьте endpoint GET /api-docs.json с res.json(specs).

#### Потенциальные улучшения (опционально)
- Автогенерация из Zod schemas (если используете Zod для validation) — интегрируйте zod-to-openapi.
- CI/CD: Добавьте в GitHub Actions генерацию spec на каждый push.
- Security: Защитите /api-docs JWT, если API приватный.

Если реализация готова, кинь код — я протестирую или доработаю. Или если ТЗ нужно скорректировать (добавить/убрать что-то), скажи! Готов к следующему шагу в проекте. 🚀